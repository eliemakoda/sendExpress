services:
  node-server:
    restart: always
    build: 
      context: .
    environment:
      NODE_ENV: production
    ports:
      - 5000:5000
    networks:
      - my-network
    command: bash setup.sh

networks:
  my-network:
